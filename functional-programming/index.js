console.log('script loaded')

// Methode 2: Stop alle data in een string variable

// Stap 1: Put all data in a string variable in the data folder using the template literal sign ``
// Stap 2: Load that file in html before you load this file so it's available

// fetch("data/Survey_Information_Design_clean-parsed.json")
// .then(response => response.json())
// .then(json => console.log(json));

// const surveyAnswers = data
// console.log(surveyAnswers)

// console.log(surveyAnswers[3].hoeveelheidOomsenTantes)